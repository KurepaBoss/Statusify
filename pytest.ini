[pytest]
testpaths = tests
# tests/legacy holds standalone diagnostic scripts from earlier debugging
# sessions. They call sys.exit() at import time, which aborts pytest's whole
# collection phase with an INTERNALERROR — running `pytest` in this repo
# reported "no tests ran" while every real test sat there passing.
norecursedirs = legacy __pycache__ .artcache exports
